import React from 'react';
import { StyleSheet} from 'react-native';


const FormCheckBox = props => {
   const {toggleCheckBox, setToggleCheckBox} = props;
  return (
    <>
     
    </>
  );
};

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#4285F4',
    height: 39,
    borderRadius: 8,
    fontSize: 16,
    paddingLeft: 10,
    marginBottom: 20,
  },
});

export default FormCheckBox;
